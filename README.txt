# AI-Python
Modules that need to be installed for the program to work:
pip install tensorflow
pip install tensorflow-datasets
pip install Pillow
############################################################
P.S Right mouse button clears the canvas
